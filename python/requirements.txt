# Optional capture and vision dependencies
sounddevice>=0.4.6
numpy
soundcard
opencv-python

# Core runtime (keep only if you want reproducible install guidance)
fastapi
uvicornfastapi>=0.110
uvicorn[standard]>=0.29